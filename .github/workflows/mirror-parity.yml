name: Mirror Parity

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  parity:
    uses: hairglasses-studio/.github/.github/workflows/mcp-mirror-parity.yml@main
    with:
      canonical-path: mcp/tmux-mcp
