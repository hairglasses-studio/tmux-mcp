name: Repo Hygiene

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  hygiene:
    uses: hairglasses-studio/.github/.github/workflows/repo-hygiene.yml@main
    with:
      max-root-docs: 10
