name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: hairglasses-studio/.github/.github/workflows/mcp-test.yml@main
    with:
      go-version: "1.26.1"
      expected-tool-count: 9
